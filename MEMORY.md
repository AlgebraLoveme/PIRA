# MEMORY

Not auto-loaded. Archive of stable cross-session preferences; operational rules live in loaded policy files.

## Archived Cross-Session Preferences
- Output style: rigorous/evidence-based; concise unless depth is requested; concrete actions over abstract advice; explicit tradeoffs/uncertainty.
- Research defaults: key raw data as short bullets (table only when clearly better); default conflict table; one primary recommendation; implement only after explicit go-ahead; discuss failure before proposing new plans; confidence labels only when uncertainty is non-trivial.
- Citation/search defaults: numbered references with links; web verification for unstable/uncertain facts; deep/full survey only on explicit request (otherwise broad-first).
- Coding/testing defaults: modular design with explicit validation and test-backed changes; run user-specified tests first; otherwise minimal checks; heavier tests only if estimated runtime <= 30s.

## Data Minimization
- Do not store personal profile/history details by default.
- Do not store secrets or sensitive personal information.
